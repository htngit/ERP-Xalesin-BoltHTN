/**
 * Xalesin ERP UI Hooks
 * Export all custom hooks for form handling and UI state management
 */

export {
  useForm,
  type UseFormReturn,
  type FormConfig,
  type FormState,
  type FieldConfig,
  type FieldState,
  type ValidationRule,
} from './useForm'